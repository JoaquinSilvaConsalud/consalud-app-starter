import React from 'react';
import './{{pascalCase name}}.css';

// Importar componentes directamente desde el código fuente
import { Card } from '../../../../consalud-ui/src/components/Card/Card';

const {{pascalCase name}}: React.FC = () => {
  return (
    <div className="{{kebabCase name}}">
      <div className="{{kebabCase name}}__container">
        <Card className="{{kebabCase name}}__document">
          <header className="{{kebabCase name}}__header">
            <h1 className="{{kebabCase name}}__title">
              {{pascalCase name}}
            </h1>
            <div className="{{kebabCase name}}__meta">
              <span className="{{kebabCase name}}__date">Fecha: {new Date().toLocaleDateString()}</span>
            </div>
          </header>

          <div className="{{kebabCase name}}__content">
            <section className="{{kebabCase name}}__section">
              <h2 className="{{kebabCase name}}__section-title">Introducción</h2>
              <p className="{{kebabCase name}}__paragraph">
                Este es un documento de ejemplo. Puedes personalizar este contenido según tus necesidades.
              </p>
            </section>

            <section className="{{kebabCase name}}__section">
              <h2 className="{{kebabCase name}}__section-title">Contenido Principal</h2>
              <p className="{{kebabCase name}}__paragraph">
                Aquí puedes agregar el contenido principal del documento. 
                El diseño está optimizado para la lectura de documentos largos.
              </p>
              <ul className="{{kebabCase name}}__list">
                <li>Punto de lista 1</li>
                <li>Punto de lista 2</li>
                <li>Punto de lista 3</li>
              </ul>
            </section>

            <section className="{{kebabCase name}}__section">
              <h2 className="{{kebabCase name}}__section-title">Conclusión</h2>
              <p className="{{kebabCase name}}__paragraph">
                Agrega aquí las conclusiones o resumen del documento.
              </p>
            </section>
          </div>
        </Card>
      </div>
    </div>
  );
};

export default {{pascalCase name}};

